---
phase: 03-readme-documentation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05]
files_modified:
  - README.md
autonomous: true
---

<objective>
Reconcile README.md to the current repository implementation.

Purpose: the README already exists, but the deployment architecture and command
surface changed during debugging. This plan updates README.md so Phase 3 status
matches reality instead of the older Phase 1 assumptions.
Output: a README that reflects the current `nemotron.py` behavior, deployment
commands, CPU/GPU endpoint split, and known limitations.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update quickstart and architecture sections to match current code</name>
  <files>README.md</files>
  <action>
Refresh the README so it reflects the current code instead of the original
single-endpoint llama-server design.

Required updates:
- Use `flash deploy`, not `flash deploy nemotron.py`
- Keep `python nemotron.py seed` as the one-time remote seeding flow
- Document that the repository currently exposes a CPU utility API and a GPU
  inference API as separate load-balanced endpoints
- Remove stale claims about a localhost proxy or `llama-server` if the code now
  uses direct `llama_cpp` loading
- Keep the README honest about what has and has not been verified on live RunPod
  so Phase 3 does not overstate completion
  </action>
  <verify>rg -n "flash deploy$|python nemotron.py seed|CPU|GPU|chat/completions" README.md</verify>
  <done>README command examples and architecture notes match the current repository implementation</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile operational guidance and limitations</name>
  <files>README.md</files>
  <action>
Update the operational sections so they reflect the current reality of the
project:
- Cold-start explanation still relevant
- Cost notes still present
- Explicitly note that the license file is still outstanding
- Call out current live-debug risks or known limitations discovered during
  deployment work if they are still unresolved
  </action>
  <verify>rg -n "cold start|cost|license|limitation" README.md</verify>
  <done>README limitations and operational guidance are aligned with the current repo state</done>
</task>

</tasks>

<verification>
- [ ] `rg -n "flash deploy$|python nemotron.py seed|CPU|GPU|chat/completions" README.md` passes
- [ ] `rg -n "cold start|cost|license|limitation" README.md` passes
- [ ] README no longer claims the old `flash deploy nemotron.py` flow
- [ ] README no longer describes the outdated llama-server proxy architecture if the code no longer uses it
</verification>

<success_criteria>
- README reflects the current deployment and seeding commands
- README documents the current split between CPU utility routes and GPU inference routes
- README cost/cold-start guidance is still present
- README does not overclaim verification that has not yet happened
</success_criteria>

<output>
After completion, create `.planning/phases/03-readme-documentation/03-01-SUMMARY.md`
</output>
