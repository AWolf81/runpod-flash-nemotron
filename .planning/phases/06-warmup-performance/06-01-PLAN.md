---
phase: 06-warmup-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/install_llama_server.sh
  - docs/benchmarks.md
autonomous: true

must_haves:
  truths:
    - "Cold start time (idle → model ready) is measured and documented with warm binary on volume"
    - "Network volume read throughput is benchmarked and confirmed as bottleneck or not"
    - "--mlock evaluated: either added to startup flags (if beneficial) or ruled out with reason"
  artifacts:
    - path: "docs/benchmarks.md"
      provides: "Benchmark results: cold start timing, volume throughput, mlock evaluation"
      min_lines: 20
    - path: "patches/install_llama_server.sh"
      provides: "llama-server startup flags (mlock added if evaluation shows benefit)"
  key_links:
    - from: "docs/benchmarks.md"
      to: "patches/install_llama_server.sh"
      via: "--mlock recommendation applied or documented as deferred"
---

<objective>
Measure and diagnose idle-to-ready time for the Nemotron endpoint with a warm binary on the network volume, then evaluate whether --mlock can reduce VRAM re-paging on repeat cold starts.

Purpose: The open benchmark from Phase 5 — aborted at 6 min — needs a clean measurement with the binary already cached. This determines whether the 3-6 min VRAM load is a volume throughput problem or a CUDA mmap page-fault problem, and whether --mlock is a viable mitigation.

Output:
- docs/benchmarks.md: cold start time, volume dd throughput, mlock evaluation with results
- patches/install_llama_server.sh: --mlock added to auto_warmup() call if evaluation confirms it helps
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-model-caching/05-01-SUMMARY.md
@nemotron.py
@patches/install_llama_server.sh
@warmup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark network volume read throughput</name>
  <files>docs/benchmarks.md</files>
  <action>
Create docs/benchmarks.md. Add a section "Network Volume Throughput" with the dd command and placeholder for results.

The actual benchmark must be run on a live RunPod worker (not locally) since /runpod-volume doesn't exist locally. Write the benchmark script inline in docs/benchmarks.md so the developer can run it manually or via seed job.

Throughput benchmark command:
```bash
# Run on RunPod worker (e.g. via seed job or manual SSH)
dd if=/runpod-volume/models/UD-Q4_K_XL/NVIDIA-Nemotron-3-Super-120B-A12B-UD-Q4_K_XL-00001-of-00003.gguf \
   of=/dev/null bs=128M iflag=direct 2>&1 | tail -1
```

Document expected range: network volumes on RunPod typically deliver 200-800 MB/s. At 200 MB/s, reading ~60 GB takes ~5 min — matching the observed 3-6 min load time. This would confirm volume throughput IS the bottleneck.

Add "How to run" section with exact steps:
1. Ensure binary is seeded (`python nemotron.py seed`)
2. Deploy (`flash deploy`) and get endpoint URL
3. SSH into worker or use RunPod console to run the dd command
4. Record output in this file

Also add context: mmap load time = model_size / volume_throughput. At 60 GB and 200-400 MB/s → 2.5-5 min is expected even with perfect mmap.
  </action>
  <verify>docs/benchmarks.md exists with throughput benchmark section, dd command, and expected analysis</verify>
  <done>docs/benchmarks.md created with runnable throughput benchmark and analysis framework</done>
</task>

<task type="auto">
  <name>Task 2: Document cold start timing methodology</name>
  <files>docs/benchmarks.md</files>
  <action>
Add "Cold Start Timing" section to docs/benchmarks.md.

Cold start = from RunPod worker boot to llama-server first token. The install_llama_server.sh script already handles:
1. Binary restore from volume (fast, ~1s)
2. auto_warmup() starts llama-server in background
3. warmup.sh polls /health until llama_server_ready=true

The timing methodology:
- Use warmup.sh output timestamps (it already polls with elapsed time)
- Or use Flash SDK `execution_timeout` plus RunPod worker logs
- Phase 5 aborted measurement at 6 min; document this as the upper bound observed

Add timing command:
```bash
# Run from local machine after deploy
time bash warmup.sh https://<ENDPOINT_ID>-8080.proxy.runpod.net
```

warmup.sh already prints elapsed time per poll. The final "ready" timestamp IS the cold start duration.

Document what to record:
- Binary restore: check install_llama_server.sh output (instant if cached, ~5-10 min if build)
- Model load: time from "starting llama-server" to "llama_server_ready=true" in /health
- Total: warmup.sh wall time

Add a Results table with columns: Date | Binary Status | Model Load Time | Total Cold Start | Volume Throughput | Notes
  </action>
  <verify>docs/benchmarks.md has cold start timing section with warmup.sh methodology and results table</verify>
  <done>Cold start measurement methodology documented; developer can run and record results</done>
</task>

<task type="auto">
  <name>Task 3: Evaluate --mlock and update startup flags if beneficial</name>
  <files>docs/benchmarks.md, patches/install_llama_server.sh</files>
  <action>
Evaluate --mlock for llama-server and document findings in docs/benchmarks.md. Then update install_llama_server.sh accordingly.

**What --mlock does:**
`--mlock` (maps to `mlock()` syscall) pins model pages in physical RAM after first fault. On the SAME worker instance: second cold start skips re-paging since pages are already pinned. On a NEW worker (scale-to-zero boot): no benefit — pages were never loaded.

**VRAM context for this model:**
- Model weights: ~60 GB VRAM (mmap'd from volume)
- KV cache: ~4-8 GB VRAM (at 32768 ctx)
- CUDA overhead: ~3-5 GB
- Total: ~67-73 GB of 97 GB VRAM used
- Remaining: ~24-30 GB headroom

**mlock evaluation:**
- `--mlock` pins pages in system RAM, not VRAM. GPU tensors are already in VRAM.
- For GGUF mmap: CPU pages (the memory-mapped file) get pinned, but the bottleneck is DMA transfer to VRAM, not page faults.
- **Verdict:** --mlock is unlikely to help for VRAM load time. The bottleneck is volume read speed → DMA to GPU, not CPU page fault resolution.

However, `--mlock` DOES help if the model is on local NVMe (prevents eviction). On network volume with mmap, it may slow things down by forcing page population upfront rather than on-demand.

**Decision:**
- Do NOT add --mlock to auto_warmup() in install_llama_server.sh
- Document why in docs/benchmarks.md under "--mlock Evaluation" section
- Note: if RunPod ever offers local NVMe model storage, --mlock should be re-evaluated

Add "--mlock Evaluation" section to docs/benchmarks.md with this analysis.

**No change to install_llama_server.sh** — leave auto_warmup() flags as-is (mmap without mlock is correct for network volume).

Existing auto_warmup() flags to preserve:
```
--n-gpu-layers 99
--parallel 1
--ctx-size 32768
--flash-attn on
```
  </action>
  <verify>docs/benchmarks.md has --mlock evaluation section; install_llama_server.sh auto_warmup() flags are unchanged</verify>
  <done>--mlock evaluated and documented; no flag change warranted for network volume scenario</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/benchmarks.md exists with all three sections: Volume Throughput, Cold Start Timing, --mlock Evaluation
- [ ] docs/benchmarks.md contains runnable dd benchmark command
- [ ] docs/benchmarks.md contains warmup.sh timing methodology
- [ ] docs/benchmarks.md has results table (empty rows OK — filled by developer after live run)
- [ ] --mlock evaluation explains why it doesn't help for network volume + VRAM loading
- [ ] install_llama_server.sh auto_warmup() flags unchanged (no regression)
</verification>

<success_criteria>

- All tasks completed
- docs/benchmarks.md is a self-contained reference a developer can use to reproduce benchmarks
- The 3-6 min VRAM load mystery is explained (volume throughput math) even without a live measurement
- --mlock decision is documented with reasoning
- No regressions introduced to nemotron.py or install_llama_server.sh
  </success_criteria>

<output>
After completion, create `.planning/phases/06-warmup-performance/06-01-SUMMARY.md`
</output>
