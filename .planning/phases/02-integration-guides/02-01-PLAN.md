---
phase: 02-integration-guides
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INTG-01, INTG-02, INTG-03]
files_modified:
  - docs/integrations/claude-code.md
  - docs/integrations/opencode.md
  - docs/integrations/mistral-vibe.md
  - examples/claude-code/litellm.config.yaml
  - examples/opencode/opencode.json
  - examples/mistral-vibe/config.toml
autonomous: true

must_haves:
  truths:
    - "Developer has a copy-paste integration path for Claude Code, OpenCode, and Mistral Vibe"
    - "Claude Code guidance is technically correct: it uses an Anthropic-format gateway shim rather than pretending the RunPod OpenAI endpoint works directly"
    - "OpenCode guidance uses the current upstream config path and schema"
    - "Mistral Vibe guidance uses a valid generic OpenAI provider definition in config.toml"
    - "Every integration includes explicit placeholder variables for endpoint URL, API key, and model selection"
    - "Example config files are parseable so documentation drift is detectable during execution"
  artifacts:
    - path: "docs/integrations/claude-code.md"
      provides: "Claude Code guide with LiteLLM bridge to RunPod"
      min_lines: 25
      contains: "ANTHROPIC_BASE_URL"
    - path: "docs/integrations/opencode.md"
      provides: "OpenCode guide using custom OpenAI-compatible provider"
      min_lines: 20
      contains: "@ai-sdk/openai-compatible"
    - path: "docs/integrations/mistral-vibe.md"
      provides: "Mistral Vibe guide using generic OpenAI-style provider"
      min_lines: 20
      contains: "api_style = \"openai\""
    - path: "examples/opencode/opencode.json"
      contains: "\"baseURL\""
    - path: "examples/mistral-vibe/config.toml"
      contains: "backend = \"generic\""
    - path: "examples/claude-code/litellm.config.yaml"
      contains: "model_list"
  key_links:
    - from: "docs/integrations/claude-code.md"
      to: "examples/claude-code/litellm.config.yaml"
      via: "copy-paste LiteLLM bridge config that fronts the RunPod endpoint"
    - from: "docs/integrations/opencode.md"
      to: "examples/opencode/opencode.json"
      via: "matching provider id, base URL placeholder, and model alias"
    - from: "docs/integrations/mistral-vibe.md"
      to: "examples/mistral-vibe/config.toml"
      via: "matching provider name, env var, and active model alias"
---

<objective>
Write the Phase 2 integration artifacts for Claude Code, OpenCode, and Mistral Vibe.

Purpose: give a first-time user working, copy-pasteable integration snippets for all three coding clients after Phase 1 deployment is live.
Output: three integration guides plus parseable example config files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-integration-guides/02-RESEARCH.md
@.planning/phases/02-integration-guides/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenCode and Mistral Vibe example configs plus matching docs</name>
  <files>docs/integrations/opencode.md, docs/integrations/mistral-vibe.md, examples/opencode/opencode.json, examples/mistral-vibe/config.toml</files>
  <action>
Create the OpenCode and Mistral Vibe integration assets.

For `examples/opencode/opencode.json`:
- Use the current upstream global config shape, not the stale `config.json` path from REQUIREMENTS.md.
- Define a custom provider using `npm: "@ai-sdk/openai-compatible"`.
- Set `options.baseURL` to a placeholder RunPod URL ending in `/v1` if that is what the client expects.
- Read the key from an env placeholder like `{env:RUNPOD_API_KEY}`.
- Define a model alias such as `nemotron-runpod`.

For `docs/integrations/opencode.md`:
- Tell the user where the file goes: `~/.config/opencode/opencode.json`.
- Include one copy-paste JSON snippet and one short validation step.
- Call out that the roadmap's older `config.json` path is outdated and the guide intentionally uses the current path.

For `examples/mistral-vibe/config.toml`:
- Create a generic OpenAI-style provider with:
  - `api_base`
  - `api_key_env_var = "RUNPOD_API_KEY"`
  - `api_style = "openai"`
  - `backend = "generic"`
- Create a model entry that points at the provider and expose a readable alias.
- Set `active_model` to that alias.

For `docs/integrations/mistral-vibe.md`:
- Tell the user where `config.toml` lives and that credentials can come from env vars or `~/.vibe/.env`.
- Include one copy-paste TOML snippet and a short smoke-test instruction.
- Keep the guide focused on the RunPod endpoint rather than generic provider theory.
  </action>
  <verify>
python3 -c "import json; json.load(open('examples/opencode/opencode.json'))" &&
python3 -c "import tomllib; tomllib.load(open('examples/mistral-vibe/config.toml','rb'))" &&
rg -n "@ai-sdk/openai-compatible|baseURL|RUNPOD_API_KEY" docs/integrations/opencode.md examples/opencode/opencode.json &&
rg -n "api_style = \"openai\"|backend = \"generic\"|RUNPOD_API_KEY" docs/integrations/mistral-vibe.md examples/mistral-vibe/config.toml
  </verify>
  <done>OpenCode and Mistral Vibe docs exist, both example configs parse successfully, and required RunPod placeholders plus provider settings are present</done>
</task>

<task type="auto">
  <name>Task 2: Create Claude Code LiteLLM bridge example and guide</name>
  <files>docs/integrations/claude-code.md, examples/claude-code/litellm.config.yaml</files>
  <action>
Create the Claude Code integration assets based on the research constraint that Claude Code needs an Anthropic-format gateway.

For `examples/claude-code/litellm.config.yaml`:
- Write a minimal LiteLLM proxy config that defines one model mapping for the RunPod backend.
- Make it explicit in comments or adjacent docs that LiteLLM fronts Claude Code and forwards to the RunPod OpenAI-compatible endpoint.
- Use placeholder env vars for the RunPod base URL, API key, and model identifier where possible.

For `docs/integrations/claude-code.md`:
- Be explicit that direct `ANTHROPIC_BASE_URL=<runpod>` will not work against a pure OpenAI-compatible endpoint.
- Provide the smallest workable flow:
  1. Install/run LiteLLM locally or in Docker.
  2. Start it with `examples/claude-code/litellm.config.yaml`.
  3. Point Claude Code at the local gateway with `ANTHROPIC_BASE_URL`.
  4. Authenticate using `ANTHROPIC_AUTH_TOKEN` in env or an `env` block in `~/.claude/settings.json`.
- Include one copy-paste env block and one `settings.json` snippet.
- Keep the guide short, but do not hide the gateway requirement.
  </action>
  <verify>
rg -n "ANTHROPIC_BASE_URL|ANTHROPIC_AUTH_TOKEN|settings.json|litellm" docs/integrations/claude-code.md &&
rg -n "model_list|api_base|RUNPOD" examples/claude-code/litellm.config.yaml
  </verify>
  <done>Claude Code guide exists, clearly explains the gateway shim, and includes copy-paste env/settings snippets plus a matching LiteLLM config example</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -c "import json; json.load(open('examples/opencode/opencode.json'))"` passes
- [ ] `python3 -c "import tomllib; tomllib.load(open('examples/mistral-vibe/config.toml','rb'))"` passes
- [ ] `rg` confirms Claude Code guide includes `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, `settings.json`, and `litellm`
- [ ] `rg` confirms OpenCode example/doc include `@ai-sdk/openai-compatible`, `baseURL`, and `RUNPOD_API_KEY`
- [ ] `rg` confirms Vibe example/doc include `api_style = "openai"`, `backend = "generic"`, and `RUNPOD_API_KEY`
- [ ] All three guides explain where the config file lives on disk
- [ ] Claude Code guide explicitly states the direct-endpoint limitation so execution does not ship a false claim
</verification>

<success_criteria>
- Three integration guides exist and are written for first-time users
- OpenCode example uses the current upstream config path and schema
- Mistral Vibe example uses a valid generic OpenAI provider definition
- Claude Code example uses a valid gateway-based approach instead of a misleading direct OpenAI claim
- All snippets are copy-pasteable and use consistent placeholder names
- Example JSON and TOML files are parseable
- No requirement ID is left uncovered
</success_criteria>

<output>
After completion, create `.planning/phases/02-integration-guides/02-01-SUMMARY.md`
</output>
