---
phase: 01-core-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [nemotron.py]
autonomous: true

must_haves:
  truths:
    - "Developer can run `flash deploy nemotron.py` and get a live RunPod endpoint URL"
    - "llama-server starts with all required flags: -ngl 99, --override-tensor exps=CPU, -c 32768, -fa, --no-mmap, -np 1, --cont-batching"
    - "execution_timeout=1800 is set with an inline comment explaining why (120B model needs >10 min)"
    - "idle_timeout is configured with an inline comment explaining billing implications"
    - "--override-tensor exps=CPU is documented with an inline comment explaining MoE memory layout"
    - "Context window is 32768 with a comment block showing the scaling ladder"
  artifacts:
    - path: "nemotron.py"
      provides: "RunPod Flash endpoint deployment configuration"
      min_lines: 30
      contains: "GpuGroup.AMPERE_80"
    - path: "nemotron.py"
      contains: "execution_timeout=1800"
    - path: "nemotron.py"
      contains: "override-tensor"
    - path: "nemotron.py"
      contains: "32768"
  key_links:
    - from: "nemotron.py"
      to: "llama-server CMD"
      via: "cmd/command parameter containing all required flags"
    - from: "nemotron.py"
      to: "network volume"
      via: "NetworkVolume configuration with model path matching download_model.py"
---

<objective>
Write nemotron.py — the RunPod Flash deployment script for Nemotron-3-Super-120B.

Purpose: Gives the developer a single `flash deploy nemotron.py` command that spins up a serverless endpoint on A100 80GB. All configuration is in this file with inline documentation so the script is self-explaining.
Output: nemotron.py — complete, runnable Flash deployment script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write endpoint configuration (GPU, volume, FlashBoot, timeouts)</name>
  <files>nemotron.py</files>
  <action>
Create nemotron.py using the runpod Python SDK. The script configures a Flash serverless endpoint with:

- `GpuGroup.AMPERE_80` — A100 80GB
- `NetworkVolume` — 100 GB, mounted at `/workspace/models` (this path must match download_model.py)
- `FlashBoot` enabled — faster cold starts
- `execution_timeout=1800` — with inline comment: "120B model needs >10 min for long responses; default 600s will kill mid-generation"
- `idle_timeout` — set to a sensible default (e.g. 60s) with inline comment: "Scale to zero after 60s idle; lower = less billing for short coding sessions, higher = faster response if switching tasks"

Use whatever import style the runpod SDK requires (check `import runpod` or `from runpod import ...`). If the SDK uses a decorator/handler pattern rather than an Endpoint class, adapt accordingly but keep all the config values identical.

Include a top-of-file docstring explaining what the script does and how to run it (`flash deploy nemotron.py`).
  </action>
  <verify>python3 -c "import ast; ast.parse(open('nemotron.py').read()); print('syntax OK')"</verify>
  <done>nemotron.py is syntactically valid and contains GpuGroup.AMPERE_80, NetworkVolume, FlashBoot, execution_timeout=1800, idle_timeout with all inline comments present</done>
</task>

<task type="auto">
  <name>Task 2: Add llama-server CMD with flags and context window comment block</name>
  <files>nemotron.py</files>
  <action>
Add the llama-server command to the endpoint configuration. The CMD must include ALL of these flags:

```
llama-server \
  -ngl 99 \
  --override-tensor "exps=CPU" \
  -c 32768 \
  -fa \
  --no-mmap \
  -np 1 \
  --cont-batching \
  --port 8080 \
  --model /workspace/models/<model-filename>
```

Inline comments required for:
- `--override-tensor "exps=CPU"` → "MoE expert weights (~80% of params) exceed VRAM; offloaded to CPU RAM. Non-expert weights + KV cache stay in VRAM."
- `-ngl 99` → "Offload all layers to GPU (use a number >= actual layer count)"
- `-fa` → "Flash Attention — required for large context without OOM"
- `--no-mmap` → "Disable memory mapping; load model fully into RAM before serving"
- `-np 1` → "One parallel slot; serverless handles concurrency by spawning instances"
- `--cont-batching` → "Continuous batching for throughput when multiple requests queue"

Add a comment block above `-c 32768` explaining the context/KV memory tradeoff:

```
# Context window options (KV cache lives in VRAM, ~320KB/token):
#   -c 32768   ~10 GB KV  — safe default, good for most coding tasks
#   -c 65536   ~20 GB KV  — larger files, multi-file context
#   -c 131072  ~40 GB KV  — experimental; test for OOM before relying on it
#   -c 200000            — likely OOM on A100 80GB with this model
# Non-expert weights use ~25-30 GB, leaving ~50 GB for KV cache.
```

The model filename pattern is `Nemotron-3-Super-120B-Instruct-UD-Q4_K_XL.gguf` (or similar — use a variable/placeholder so it's easy to update).
  </action>
  <verify>grep -c "override-tensor\|cont-batching\|32768\|exps=CPU" nemotron.py | grep -v "^0$"</verify>
  <done>nemotron.py contains llama-server CMD with all required flags, all inline comments, and the context scaling comment block</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -c "import ast; ast.parse(open('nemotron.py').read())" passes
- [ ] grep confirms: GpuGroup.AMPERE_80, execution_timeout=1800, override-tensor, cont-batching, 32768, idle_timeout all present
- [ ] Inline comments exist for execution_timeout, idle_timeout, --override-tensor, and the context window block
</verification>

<success_criteria>
- nemotron.py is syntactically valid Python
- All required RunPod config present: GPU group, network volume, FlashBoot, both timeouts
- llama-server CMD contains all 8 required flags
- All inline documentation comments written
- Context window comment block explains the scaling ladder and memory math
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-deployment/01-01-SUMMARY.md`
</output>
